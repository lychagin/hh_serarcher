from collections.abc import Sequence
from datetime import datetime
from typing import Protocol

from hh_search.domain.models import ScoredVacancy

# Формат даты в отчётах: без микросекунд и без смещения. Из базы даты
# приходят как aware UTC (`storage/time_utils.py`), то есть isoformat() дал
# бы «2026-07-27T11:48:48.366000+00:00» — Excel такую строку числом не
# считает, а человеку она нечитаема. Время в отчёте — UTC, как в базе.
REPORT_DATE_FORMAT = "%Y-%m-%d %H:%M"

# Сколько СУТОК назад читать файлы отчёта ради дедупликации, помимо
# сегодняшнего (спека §5, item 3). Одних вчерашних было мало: отказ
# `send_document` вечером 29-го и повторный отказ ДОВОЗКИ (item 1) утром
# 30-го оставляют вакансию непомеченной ещё одни сутки, и прогон 31-го,
# заглянув только на день назад (в пустой файл 30-го — довозка ничего не
# пишет), не находит её нигде и повторяет вечернее сообщение дословно.
# Двух суток достаточно ровно для этого сценария: прогон 31-го видит файлы
# 30-го И 29-го.
#
# Шире — не значит безопаснее. `mark <id> new` возвращает вакансию в
# очередь, обнулив счётчик попыток, и если её ссылка совпадёт с файлом
# ПЯТИДНЕВНОЙ давности, широкое окно молча проглотит вручную возвращённую
# вакансию — находка ревью, которую сторожит
# `test_vacancy_outside_the_lookback_window_is_not_suppressed`. Окно
# намеренно узкое: ровно столько, сколько нужно для воспроизведённого
# сценария, и ни днём больше.
#
# Живёт здесь, а не в `telegram_sink.py`: константа нужна и приёмнику
# (дедупликация, довозка), и конвейеру уборки (`pipeline/report_files.py`,
# защищённое окно `PROTECTED_DAYS`), а конвейер зависит от границы слоя —
# протокола `Sink` — а не от конкретного приёмника (§4.3 спеки
# `docs/superpowers/specs/2026-07-27-hh-autosearch-design.md`; тот же
# приём, что разводит `storage/base.py` и `storage/repository.py`).
LOOKBACK_DAYS = 2


class Sink(Protocol):
    """Точка расширения: сюда позже встанет TelegramSink."""

    name: str

    def emit(self, vacancies: Sequence[ScoredVacancy], now: datetime) -> int:
        """Отдать вакансии приёмнику. Возвращает, сколько ЗАПИСАНО.

        Не «сколько отдано»: оба файловых приёмника дедуплицируют по
        содержимому файла дня, поэтому пачка из 143 вакансий регулярно
        превращается в ноль новых строк. Пока число не возвращалось,
        команда `report` печатала «перегенерировано вакансий: 143», не
        записав ни байта (замер: размеры файлов до и после совпали до
        байта), — сообщение называло не то, что произошло.
        """
        ...

    def maintain(self, now: datetime) -> None:
        """Обслужить приёмник — независимо от того, есть ли что отправлять.

        Зовётся `report()` ДО раннего возврата при пустой очереди, то есть
        и в прогонах, где отправлять нечего. Сюда попадает работа, которую
        нельзя привязывать к наличию вакансий: у `telegram` это уборка
        черновиков и довозка документов, застрявших без отметки о
        доставке.

        Отказ отсюда не понижает статус прогона: недоступный транспорт
        красил бы `partial` каждый прогон подряд, а самоизлечение и так на
        месте — признак застрявшего документа лежит на диске.
        """
        ...
